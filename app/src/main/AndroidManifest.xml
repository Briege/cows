<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.strath.mydairyfarm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".CowActivity"
            android:label="My Cows"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".VaccinationActivity" />
        <activity android:name=".DewormingActivity" />
        <activity android:name=".TreatmentActivity" />
        <activity
            android:name=".Main2Activity"
            android:label="MyDairyFarm"
            android:theme="@style/AppTheme" />
        <activity android:name=".VaccinationsDone" />
        <activity android:name=".MilkCollectionActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".CowDetailActivity"></activity>
    </application>

</manifest>